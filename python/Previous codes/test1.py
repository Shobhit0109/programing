"""
p = print
print = 0
p(print)
"""
def optimumDistance(a, b, c, points, n):
    def findRange(points):
        return x,y
    def total(p):
        sum = 0
        for i in points:
            sum += (lambda p1,p2 : ((p1[0]+p2[0])**2 + (p1[0]+p2[0])**2)**0.5)(p,i)
        return sum
    
	#inclomplete doing by hit and trial on every int point
    #for evry int point calculate its distance from evry point then find max and print
    #range of calc point on line should be between max x,y coordinate of points inside point array
    
    min.sort()
    return min[0]