//ctrl+shift+a --> multiline comments ; ctrl+/ --> single line comments
//#include <bits/stdc++.h>
#include <iostream>
#include <cstdio>
using namespace std;

#define f(i,n) for (int i = 0; i < (n); i++)
#define i(i,j,n) ((i)*(n) + (j))
#define endl ("\n")

int main(void) {//int argc,char **argv) {

//     ios_base::sync_with_stdio(false);
//     cin.tie(NULL);
//     cout.tie(0);

//    #ifndef ONLINE_JUDGE

//        freopen("/home/shobhit/Documents/programing/All codes Execution files/Input Output/input.txt", "r", stdin);
//        freopen("/home/shobhit/Documents/programing/All codes Execution files/Input Output/output.txt", "w", stdout);

//    #endif
/* //No error
    int input[10];
    scanf ("%s",(char*)input);                      
    printf("%s",(char*)input);
 */
/* 
    cin >> (char*)argv;
    cout << (char*)argv;
    //this will add a new argument that we will pass to argv

 */
/*     //we can pass argument during compiling a.out file 
    cout << argc;

    while(argc) 
        cout << (argv)[argc---1] << endl;
 */
/* 
    // double array used as single array
    scanf("%s",(char*)argv);
    printf("%s",(char*)argv);
 */
/* 
    char a[][10] = {"abc","bcd","efg"};

    scanf("%s",(char*)a); //how this works?
    // equivalent to scanf("%s",a[0]);

    for (int i=0;i<3;i++)
        printf("%s\n",a[i]);
    //both printf same result
    for (int i=0;i<3;i++)
        printf("%s\n",(char*)(a+i));
  */   
 return 0;
}