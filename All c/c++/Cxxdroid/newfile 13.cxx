#include <iostream>
#include <stdio.h>
using namespace std;

int main(int argc, char *argv[])
{
	int n;
	cin >> n;
	cout << n;//how input of integer works and what it do to other cahtacters after it is inputted
	//like string gets input until white spaces 
	char a[100];
	cin >> a;
	cout << a;
	char b[100];
	cin  >> b;
	cout << b;
	return 0;
}